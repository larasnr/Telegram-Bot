# Fuentes
Lista de endpoints :)

## Endpoints
* GET /colecciones
* GET /colecciones/{nombre}
* POST /colecciones
* GET /colecciones/{nombre}/hechos
* GET /hechos/{id}
* PATCH /hechos/{id}  {“estado”: “borrado”}
